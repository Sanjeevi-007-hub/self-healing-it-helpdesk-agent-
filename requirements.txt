streamlit>=1.36.0
chromadb>=0.5.0
sentence-transformers>=2.7.0
python-dotenv>=1.0.1

# Optional — only needed if LLM_PROVIDER=openai
openai>=1.30.0

# Optional — only needed if LLM_PROVIDER=watsonx
ibm-watsonx-ai>=1.0.0

# Testing
pytest>=8.2.0
